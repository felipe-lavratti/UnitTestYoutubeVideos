#!python
Import('env')

r = {
    'libs' : [],
    'path' : [],
    'objs' : []
}

src = env.FindFiles('./', '*.c')
src = env.FindFiles('./', '*.cpp')
lib = env.StaticLibrary('app_tests', src)

Depends(env.SourceToObjects(src), env['CPPINCLUDES'])
r['libs'].extend(lib)
Return('r')
